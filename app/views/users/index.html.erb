<h1>Listing Users</h1>
<ul>
	<% @users.each do |user| %>
		<li><%= link_to user.username, profile_user_path(user.username) %> 
			(<%= link_to 'delete', user_path(user.id), method: :delete %>)</li>
	<% end %> 
</ul>
